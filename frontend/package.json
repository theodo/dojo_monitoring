{
  "name": "dojo_monitoring",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@sentry/react": "^7.12.1",
    "@sentry/tracing": "^7.12.1",
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "dat.gui": "^0.7.7",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.2.1",
    "react-scripts": "4.0.3",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev": "yarn start",
    "prestart:prod": "yarn build",
    "start:prod": "yarn run serve -s ./build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "sentry:create-release": "sentry-cli releases new gravity@1.0.0 --finalize",
    "sentry:upload-source-maps": "sentry-cli releases files gravity@1.0.0 upload-sourcemaps --ext js --ext map ./build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "serve": "^14.0.1"
  }
}
